'use client';

import type { TDevice } from 'types/generalTypes';

export type TGetAllDevices = {
  allDevices: TDevice[];
}

export const revalidate = 1;

const Page = () => (
  <>
  </>
);

export default Page;
